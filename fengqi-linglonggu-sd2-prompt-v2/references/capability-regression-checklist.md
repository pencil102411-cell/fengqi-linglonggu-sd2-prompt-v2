# V2.3 Capability Regression Checklist

Use this reference only when maintaining, upgrading, or validating the V2 skill. Do not load it for ordinary character analysis or prompt generation.

## 1. Source-File Integrity Gate

Keep the seventeen maintained references byte-identical unless the user explicitly requests a source-reference update. When an update is intentional, mark its expected SHA-256 `PENDING_HASH`, add behavior coverage for the changed contract, and replace the placeholder with the final computed digest during the release-wide hash pass before accepting the revision.

| Source reference | Expected SHA-256 |
|---|---|
| `character-performance-bible.md` | `8D852C8176059F26A9BA89145BEF311CC8CB58A6C7D258A49777FEA133B84016` |
| `microexpression-acting-grammar.md` | `3872535DBFCD78C83F0C0E3A7711D11D211A6C6D51520280889C50D327E80007` |
| `body-action-and-contact-grammar.md` | `443C6684AEBA6AA10CC1BBCB993166AF38E3A5E99474FF796A55035B8CF8EB8F` |
| `camera-parameter-presets.md` | `76A43DBCA1ACBD6E07649A464C0A44F1A5B0AF619D1ADCDED481F915F9C87D5B` |
| `narrative-lighting-design.md` | `AA70CD4F84A1287CD9B77D18E1E5F48B89A7F64373A531AB87580A0BA915BF16` |
| `direct-multi-reference-video-template.md` | `9B7A4229640FDBB5FEAB8A8D9284DEFB99CAB2222EC766A088B42224A5D2C6D5` |
| `zhuyu-episodes-01-10-cinematography.md` | `36A2A7C5E27E5992F27AEEA0FE1052D361DF7BE398FBA6F456EB3F409A006D38` |
| `canglanjue-episodes-01-36-visual-grammar.md` | `2DECBB36D80A8C18E70EB938AD7C58E3FABAA57D00A9D6D6F3B6F58F39897817` |
| `eastern-fantasy-tv-visual-master.md` | `D75599C59ADA749A44D11BA993907F4A82F87685398D0649E8E8E00F285C754B` |
| `acting-performance.md` | `84EF4CADB0D469980AD8AEC5E46B713E20264CCE4A95D75159870ED8B66BB773` |
| `seedance-assembly.md` | `CF6960CD82B5604F1DBE3858A1AEA3F404F72095CE13ADDB5936F7F16F044D7F` |
| `seedance-2x-generator-translation.md` | `DA3235F161D0A540CADAB804CE00471C01863BF98D8E86E73DC339C782E25DE5` |
| `video-repair-loop.md` | `03AC9C137FE7A7AED568E0A311800CEDC897EBED821E1E5FD358024982F925E2` |
| `xianxia-action-grammar.md` | `1F2C13AC0199FBB05915B87AB44035CE015DB6F38C5D78EB021E8BBAFD1E7F39` |
| `xianxia-vfx-grammar.md` | `AD81A55B71A8EC77CB074DA6D68BCB337F2228D0B4C4C20049B60D2532CD3E69` |
| `project-craft-defaults.md` | `F7121BFA441B2E6B460AC95544A724EBEFFAA1A0D075D234D1092E941042F7F6` |
| `episode-script-retrieval.md` | `F9B27695A37F7E57F880C96EFC927B5E9226FFAF9D7CCCE1DA834ECEE8B326C9` |

Also verify that `SKILL.md` directly links every source reference, contains no unresolved placeholder, stays below 500 lines where practical, and passes the official `quick_validate.py` check.

Run `python scripts/lint_prompt.py --skill-check` on every revision. It must report zero `ERROR`: no dangling external skill reference, no broken `references/` link, no orphan reference file that `SKILL.md` never routes to, and no hash drift between a reference file and the table above.

After changing `lint_prompt.py`, run `python -X utf8 scripts/test_lint_prompt.py -v`. Also invoke the linter once without `-X utf8` on Windows to prove that Chinese diagnostics and failure icons do not crash or become unreadable. The regression suite must prove that every package in a dual-version response is validated independently, and that a missing canonical shot header or duplicated inner heading is a blocking `ERROR`.

**The linter is part of the contract, not a convenience.** Whenever a revision changes a mechanically checkable rule — an outer or inner heading name, the shot-header field order, the focal bands, the close-framing fixed clause, the integer-second protocol, the handle syntax, the character ceiling, the negative budget, the visual-family or effect-branch vocabulary — update `scripts/lint_prompt.py` in the same revision and re-run it against both a known-good and a known-bad fixture. A rule the linter no longer enforces has silently degraded back into model attention, which is the failure mode this gate exists to prevent.

## 2. Non-Loss Invariants

Reject a revision if any invariant fails:

1. Preserve canonical name and alias routing, bare-name default stages, longest-phrase matching, ambiguous-title handling, low-evidence restraint, and same-face/same-soul/body-version isolation.
2. Preserve every named character and all character-specific timing, relationship behavior, prohibitions, injury/curse/possession overlays, and the rule that explicit user/script events outrank stable personality.
3. Preserve all M01-M32 presets, M03/M22/M26/M32 source-text rulings, L1-L5 intensity, T0-T4 tear contracts, hard incompatibilities, duration/state templates, and overacting limits.
4. Preserve the seven-stage action chain, eight personality modifiers, support-foot and weight-transfer physics, hand/prop/contact continuity, ancient-costume material behavior, A01-A12 cards, and action-specific negatives.
5. Preserve the project visual, dialogue, lighting, composition, frame-within-frame, foreground obstruction, prop-insert, and spatial-axis systems, including the generic female/male fallbacks for unnamed roles only.
6. Preserve the exact two-layer camera protocol **and its fence/backstage split**. The global masters hold the visible color/grain result, the axis, the dominant support policy, and complete same-scene visual/material/light facts once. Every declared shot then begins in the fixed order `镜头号→景别→焦距→光源→机位/运动→焦点/景深`, followed by its `表演 / 动作 / 承接`; shot count equals shot-header count. An unchanged `光源` says `本场景母版，无变化`, while a changed shot states only its real delta and never repeats the shared source/direction. Preserve single values, lens-family/distance/space phrases, the 85mm+ rule, depth behavior, matched reverses, action-mode boundaries, and the ban on appending the retired full tail.

   **Camera body, frame rate, shutter angle, ISO value, white-balance Kelvin, T-stop, and color-space/delivery target are resolved at full fidelity backstage and are forbidden inside `【完整可复制提示词】`.** They decide the look but expose no Seedance control interface, so in the fence they are wasted tokens. Only their visible consequences travel into the prompt: the focal phrase with its perspective result, the depth class with focus plane and visible falloff, and the color/grain description. Reject any revision that reinstates a capture value as a required fenced field, or that removes the focal millimetre value — the millimetre is retained precisely because it is paired with a mandatory visible-perspective clause. `scripts/lint_prompt.py` enforces this split; a revision that changes it must change the linter in the same pass. Keep the creative-intent disclaimer in the backstage target record and, when operator-facing, `【镜头设计摘要】`; never restore it as a fenced first-line preamble. A frozen preservation-first baseline containing an old-format camera block stays verbatim.
7. Preserve mutually exclusive project `2.39:1 active picture + 24fps cadence intent`, `逐玉` source-match `2.40:1 active picture + BT.709 output target + 25fps cadence intent`, and `苍兰诀` source-match `2.40:1 active picture + 25fps cadence intent` paths. Map exact 2.39:1/2.40:1 to an actually selectable platform ratio plus safe composition/post-crop when required, and use a confirmed platform setting or post-conform for exact frame-rate delivery. For `苍兰诀`, BT.709 may be one explicit output target but must never be claimed as detected source metadata. Keep both dramas as transferable craft rather than copied scenes, actors, plots, dialogue, spell designs, or shot sequences.
8. Preserve Direct Multi-Reference and Quick Output as real delivery modes. Directly compile final prompts from supplied character settings, scene settings, plot, written storyboards, and actual references; do not insert a scene-asset gate or automatic image-generation stage. Replace only the legacy direct field order with the 瑞宝PRO ready-state package.
9. Preserve every legacy content control—reference rejection boundaries, environmental relighting and recoloring, skin/fabric/material integration, no-cutout behavior, media restrictions, camera metadata, action, continuity, and negatives—while remapping those controls into the 瑞宝PRO inner prompt order.
10. Preserve 瑞宝PRO shot IDs, requirement records, direct reference-role mapping, stable sequence order, and dependency continuity. Treat supplied assets as optional controls with explicit positive roles and rejection boundaries, not as permission gates. Missing or mismatched scene views are expressed through positive text and narrowed reference roles; the skill does not generate, edit, save, inspect, hash, or automatically replace an image before video-prompt delivery.
11. Keep the project character layer above generic acting; use objective/obstacle/stakes/tactic/listening/voice to adapt rather than replace character canon.
12. Keep the skill self-contained: Stage 2 owns narrative-to-shot conversion, and the project-local CineVisible compiler is the fenced generator-facing wording owner for **both** the 2.0 and the 2.5 target. Reject any revision that reintroduces an external companion-skill dependency for those roles. V2 continues to own the inner nine-heading schema beginning with `参考图角色分工：`, full reference fields, natural-Chinese form, 5000-character ceiling, opening-state field, one-to-four-shot single-block rule, inter-shot continuity, the shared global camera/light masters, and canonical front-loaded shot headers followed by performance/action/handoff prose; target/model/platform/audio/camera-intent settings remain backstage and, when operator-facing, in `【镜头设计摘要】`. Never import a JSON-like state machine, mandatory named anchors, a `生成模式与有效参考` metadata line, or a collapsed opening state. Keep `seedance-assembly.md` as the sole 瑞宝PRO outer-package owner. Never introduce an asset gate, automatic-image detour, another model profile, or a competing outer format.
12a. Keep [xianxia-action-grammar.md](xianxia-action-grammar.md) and [xianxia-vfx-grammar.md](xianxia-vfx-grammar.md) as **pure craft modules**, loaded only by their routing conditions. They own impact/trajectory/flight physics and effect spatial structure respectively; the sword's three-identity table with its handoff vocabulary, the three-layer VFX separation, effect-light binding, silhouette locking, and precise small-implement targeting are non-loss content. Reject any revision that lets either file define a workflow, an output template, a time-segment skeleton, a competing shot organization, block names such as 【时间轴】 or 【固定约束】, or a bracketed `[0-3s]` range that violates the integer-second protocol. `body-action-and-contact-grammar.md` remains the owner of the seven-stage chain and two-person contact; `camera-parameter-presets.md` remains the sole camera schema. A no-effect prompt contains none of the VFX module's vocabulary.
13. Preserve the narrative-lighting contract distilled from all three supplied modules: story/color relationship, motivated source, pre-compilation world-space placement, camera-to-key relation, apparent size and light quality, fill/negative-fill ratios, book-light causality, bounce-surface response, relevant spill boundaries, practical/background hierarchy, exposure and material response, moving light zones, and cross-shot continuity.
14. Keep [camera-parameter-presets.md](camera-parameter-presets.md) as the sole normative two-layer camera schema and [seedance-assembly.md](seedance-assembly.md) as the sole normative outer package. No other file may define a competing header, restore a literal full camera tail, or move photography after performance; every complete prompt retains all nine ordered inner headings, using truthful `无/不适用` values when necessary.
15. Direct compilation never calls image generation merely because a supplied scene reference lacks the requested exact angle, crop, landmark, interaction surface, light, or close detail. Keep that asset's role limited to the dimensions it truthfully supplies and express the missing visible result through the user's scene setting, plot, written storyboard, and positive prompt text. Bind only actual supplied files to `@图片 N / @视频 N / @音频 N`; never invent a handle or claim that an absent image was generated, saved, inspected, or accepted.
16. Preserve the complete episodes 1-24 corpus as four byte-identical canonical PDFs plus 24 mechanically extracted episode texts, a deterministic scene index, manifest hashes, and a validated lookup tool. Retrieval metadata stays backstage; only a source-verified, current-scene acting patch may reach performance routing or prompt assembly, and retrieved dialogue enters a prompt only when the user supplied or explicitly requested it.
17. Preserve the exact close-framing detail clause `角色毛孔真实细腻，双眼保持清晰眼神光`: every character-bearing `中近景 / 近景 / 特写 / 大特写 / 过肩中近景 / 过肩近景` must contain it once inside that shot's own `表演 / 动作 / 承接` prose after the camera header; in over-the-shoulder coverage it governs the in-focus face, not the defocused foreground shoulder. Do not count a global master, continuity lock, negative list, or camera header as satisfying the rule, and exempt only a prop/body-part insert with no readable face or a higher-priority explicit closed/occluded-eye requirement.
18. Preserve generated-video repair as an evidence-first mode: inspect the exact submitted prompt and returned video, keep the failed video diagnostic-only, tie each claimed cause to timestamped visible evidence plus an exact prompt clause/omission/conflict/overload or honest uncertainty, preserve successful controls, and rewrite changed supplied-reference roles directly. In ordinary unrestricted repair, deliver one complete rewritten ready-state package, including `MODEL_EXECUTION_VARIANCE`, with the five-total negative budget overriding imported legacy quotas. Under an explicit preservation-first scope lock, instead preserve the exact baseline and use exact splicing or exact patch-only output; the negative budget applies only inside the allowed span. No repair route invokes a scene-asset gate or automatic image generation.
19. Preserve the generator-prompt length contract for every complete generator prompt, including non-patch preservation-first repair: count the exact fenced copy text including whitespace and keep it at or below the 5000-character hard ceiling, with no overflow tolerance. Keep all nine headings, essential project controls, complete handle-bound per-reference role sentences, one-to-four-shot order, supplied dialogue, required performance/action, positions, scene transitions, end-to-next-opening handoffs, continuity, shared global capture/light facts, and complete compact shot headers. When an ordinary first draft exceeds 5000, remove accidental duplication and compress lower-priority expression in place until it fits; character count alone never authorizes another package. Split only for a verified hard duration limit, verified hard reference-capacity limit, explicit user request, or returned-video evidence of execution overload followed by user approval. In preservation-first repair, never alter or split frozen text merely to fit: if a non-patch complete baseline exceeds 5000 and the allowed span cannot lawfully reduce it, stop for expanded compression-scope authorization or switch to explicitly requested patch-only output. The deliberate outer/inner repetition of `参考图角色分工：` is required and never counts as removable duplication. Quick Output remains standalone and cannot be prepended verbatim to a ready-state prompt.
20. Preserve the opt-in `苍兰诀` comparator boundary and its 36-episode technical evidence baseline: one selected visual family and first visual read, explicit VFX `none` for ordinary scenes, one effect story function when active, source/trigger-to-residue causal state chain, surface-topology/depth/occlusion/contact rules, interaction light and body/material/environment response, locked-camera baseline, trigger-path-stop movement grammar, peak-response limit, top-view paragraph/axis reset, and the ban on exact equipment/metadata claims or copied source content. It must not replace character canon, direct-compilation ownership, narrative-lighting ownership, the sole global-master/per-shot-header camera schema, or the outer package.
21. Preserve the full reference-role wording contract for every reference-bound prompt: both the outer `【参考素材角色表】` and fenced first heading contain the exact legacy-compatible label `参考图角色分工：`; every actual image/video/audio receives its correct `@图片 N / @视频 N / @音频 N` upload-order handle, while every active 2.5-only clay/white-model/edit reference retains the exact handle exposed by the current product. Every asset also receives one complete natural-language `只锁定……，不复制……。` sentence with its full positive lock list, full rejection boundary, and material time span when needed. Do not shorten this content into internal role codes, filename-only entries, slash tags, fragments, pointers to the outer table, merged assets, or omitted repeated-role assets. Compress accidental repetition elsewhere rather than abbreviating these sentences or splitting for character count.
22. Keep the Seedance 2.x CineVisible compiler subordinate to that full-reference contract for both 2.0 and 2.5. It must retain the exact `参考图角色分工：` label, canonical Chinese handle, and complete natural-language sentence for every supplied reference in both outer and inner locations. Reject `@Image1`, `@Video1`, `@图片 1只锁人物身份`, the backstage `首要职责 / 控制 / 不得覆盖` record, or another capacity-oriented shorthand as final generator text.
23. Preserve target-profile routing and CineVisible translation as a separate non-loss layer: explicit 2.0/2.5 wins, explicit dual-version work produces two independent packages, retries/extensions preserve the source version, bare short `SD2` remains backward-compatible with 2.0, and otherwise-unspecified 16-30-second or over-2.0-capacity work may resolve to 2.5. Enforce the verified 2.0 `15 seconds + 9 images / 3 videos / 3 audio` and 2.5 `30 seconds + 30 images / 10 videos / 10 audio` limits without treating capacity as a recommendation. The backstage target record and `【镜头设计摘要】` separate real platform ratio, active-picture composition/crop target, synchronous-audio choice, and camera-intent classification; the fenced first line is always `参考图角色分工：` and never an operational metadata preamble. Preserve the same canon, direct reference roles, motivated light, effect active/none chain, shared global capture/light masters, front-loaded shot headers, physical response, and source-match route across versions; 2.5 may add timed macro beats, scoped references, extension, or targeted-edit structure only when supported and requested. Do not present project camera metadata or the 5000-character contract as universal model guarantees, bind handles to absent assets, or assume 2.5 solves complex physics and dense multi-subject interaction.
24. Preserve [eastern-fantasy-tv-visual-master.md](eastern-fantasy-tv-visual-master.md) as the always-on generator-facing quality baseline for every complete generation-ready prompt and complete ordinary repair. `全局视觉 / 材质 / 光影母版` contains the exact `FQ-EFTV-01` text once, then exactly one resolved `高调灵境 / 青蓝暗域 / 暖木人间 / 临界大战` family, exactly one resolved `effect=active/none` branch, and current-scene light/material facts. Keep the version code backstage; never replace the visible controls with `苍兰诀风格`, repeat the baseline per shot, or delete it for budget. The no-effect branch contains only real-world light/action/material change and no effect vocabulary; the active branch retains causal anchor/integration while leaving exact geometry to the event fields. A repair preserves the accepted family and branch unless explicit visual/effect scope or timestamped causal evidence authorizes a change.
25. Preserve the generator-visible integer-second protocol across direct compilation, both target-version writers, reference roles, timed spines, targeted edits, and complete ordinary repair. Every explicit range inside `【完整可复制提示词】` or a newly authored replacement span uses integer endpoints, one ASCII hyphen, and one final lowercase `s`, such as `1-3s` and `4-5s`; decimal seconds, colon timecodes, `~ / ～ / – / — / 至 / 到`, and Chinese `秒` suffixes are forbidden there. Do not manufacture ranges when causal order is sufficient. Keep inspected frame-accurate evidence in the outer diagnosis, keep non-time values such as `T2.8`, `2.39:1`, and fps untouched, and never normalize frozen preservation-first text outside the authorized span.

## 3. Required Behavior Regression Cases

Run representative cases without telling the executing agent the intended answer.

### Character And Acting

- `帝鸿雪夜见玲珑受伤，6秒近景，眼红不落泪，只要表演分析` -> use the correct stable stage, one counterforce, L/T controls, small protective action, and no final prompt because the user requested analysis only.
- `燕十三安慰钟离玲珑` and `崔珏面对当前玲珑` -> produce visibly different warmth, distance, and timing.
- `司徒陌看见帝鸿抱玲珑` -> retain the sweet mask, expose only one brief hardening, then use an intimacy-coded intervention.
- `小九把最后一块饼给幼年小奎` -> route to the famine incarnation with visible hunger physiology rather than current 玲珑.
- `绍华看着雪里` -> keep the same-face visual relation while rejecting 玲珑 identity, modern rhythm, and romance overlays.
- `主上回头` without context -> do not guess between 庚辰 and 混沌之主.
- `第23集后的广成子` -> use adult body scale and gait while retaining mature medical precision.
- A literal episode dialogue fragment -> locate the unique scene, reopen the indicated episode text, and use its current relationship/body/knowledge state for acting without copying locator metadata into the answer.
- A paraphrased storyboard -> rank candidates, verify the best candidate against the episode text and adjacent beat, and keep confidence conservative; a fuzzy score alone never becomes a plot fact.
- A paragraph stitched from multiple scenes -> retain separate source contexts and never leak later knowledge, injury, relationship, or emotional endpoints into an earlier beat.

### Microexpression And Body Physics

- `克制愤怒，眼红但无泪` -> use the M03 ruling and one compatible T0/T1 state; reject staring, tears, face covering, and whole-body shaking.
- `双人掌掴` -> lock striking hand, impacted cheek, support foot, kinetic chain, force direction, delayed hair response, recovery, and unchanged screen sides.
- `拥抱 / 递物 / 饮茶 / 转身` -> preserve unique contact or support points, prop hand, inertia, and a next-shot-ready end anchor.

### Camera And Source Match

- One unnumbered shot -> output exactly one front-loaded camera header with all five ordered fields, followed by one complete performance/action/handoff line.
- Four declared shots including an insert -> output exactly one ready-state package and one fenced block containing one shared global capture/light master plus four compact front-loaded camera headers, each in `镜头号→景别→焦距→光源→机位/运动→焦点/景深` order with exact single values and followed by its own performance/action/handoff prose; preserve every end-to-next-opening handoff and keep the complete fenced text at or below 5000 characters without character-count-driven splitting.
- A medium or tighter project shot with only a supplied panorama -> keep 85mm+ and shallow depth of field; limit the panorama to truthful environment/palette/material control and use text for the requested close view instead of widening, blocking, or generating a replacement plate.
- A character-bearing `过肩中近景` followed by a `近景` and a hand-prop `大特写` -> place `角色毛孔真实细腻，双眼保持清晰眼神光` exactly once inside each of the first two `表演 / 动作 / 承接` lines after their camera headers, apply the over-the-shoulder phrase to the in-focus face, and do not inject it into the hand-prop shot with no readable face.
- `逐玉原片质感` -> use a 2.40:1 active-picture target, BT.709 output target, and one 25fps cadence intent; map to a confirmed product setting or explicit post-crop/post-conform workflow and do not also write 24fps.

### 苍兰诀 Comparator And VFX

- `风起玲珑骨，Seedance 2.0，10秒，暖木室内双人对话，无术法` with no named comparator -> compile the complete fixed Eastern-fantasy television quality baseline once, resolve only `暖木人间`, select only the reality-change `effect=none` branch, append current window/practical-light facts, keep all effect vocabulary plus `FQ-EFTV-01` and `苍兰诀` out of generator-facing text, and introduce no decorative VFX.
- `苍兰诀主对标，风起玲珑骨暖木室内双人对话，无术法` -> select the grounded warm-wood family, keep the stable relationship axis and motivated practical/window light, set the effect function to `none`, add no particles, aura, energy fog, environmental recolor, or camera shake, and retain the project 2.39:1/24fps technical baseline because no source-match lock was requested.
- `苍兰诀主对标，黑烟沿白衣袖口和肩部纹理蔓延` without `原片质感 / 原片技术基线 / 2.40:1 / 25fps` -> use the comparator's surface-effect craft but retain project 2.39:1/24fps; a comparator name alone never switches the technical path.
- `苍兰诀主对标，帝鸿雪夜抬手在玲珑前撑起半球结界，冲击后结界衰减` -> retain project identity and story facts; define one defensive function, exact hand/world anchor, boundary/thickness before particles, pre-cue/birth/contact/peak/decay/residue, foreground/midground/background occlusion, interaction light, body/fabric/snow response, next-shot-ready end state, and a locked camera or one short peak response.
- `黑烟沿白衣袖口和肩部纹理蔓延` -> bind propagation to cloth curvature, seams, folds, raised edges, and partial self-occlusion; preserve skin/identity/costume texture, use a locked camera, and reject a screen-space sticker floating off the surface.
- `水系法术，雨夜落地` -> align gravity, rain/water direction, wet hair, cloth weight and delay, wet-ground reflection, mist, ripples, interaction light, and residue; do not substitute blue fog for water physics.
- `苍兰诀原片质感` -> use 2.40:1 active picture and exactly 25fps; if a color baseline is required, resolve one explicit output target, do not claim source BT.709 metadata, and do not misread the 1920×1080 container as 16:9 active composition.
- An effect-heavy shot -> prefer a locked camera while the effect grows or changes topology. A side move needs one actor/threshold trigger, one readable parallax occluder, and one stop composition; a top/high view normally enters by a cut after the action/effect state resolves and resets the primary axis when returning to people.
- A generic non-`苍兰诀` fantasy request with no named comparator -> do not load or inject this comparator merely because magic exists.

### Seedance 2.x Generator Compilation

- `Seedance 2.0，15秒，苍兰诀主对标，帝鸿掌前结界受冲击后衰减` -> one independent 2.0 package; state the exact target, real platform ratio, composition/crop target, synchronous-audio choice, and camera-intent classification backstage and in `【镜头设计摘要】`, while the fenced copy begins with `参考图角色分工：`; remain within 15 seconds and 9/3/3 references, remove abstract style labels, and compile source anchor, depth/occlusion/contact, interaction light, force/material response, residue, one dominant camera behavior, and three to five small visible changes.
- `Seedance 2.5，24秒，玲珑携信匣穿过门廊到雨院交给来人` -> one 2.5 package with three to five broad macro ranges; every range has an opening state, event/response, executable transition trigger when present, and visible handoff; the prop hand, geography, source light, identity, and audio state never reset between ranges.
- `同一10秒暖木室内无术法对话，分别输出2.0和2.5` -> two independent ready-state packages. Preserve semantically identical story, canon, `effect=none`, motivated light, global capture/light masters, front-loaded shot headers, physical behavior, and continuity; do not inflate the 2.5 version or place both model alternatives in one fenced block.
- Bare `SD2，10秒` within 9/3/3 -> resolve to 2.0 for backward compatibility. Bare `SD2，24秒` within 30/10/10 -> resolve to 2.5 and state that single target. Explicit `SD2.0，24秒` -> split at concrete handoff states instead of silently upgrading.
- A 2.0 request with ten images, or a 2.5 request with thirty-one images -> do not silently discard references or invent a scene-asset status; use a user-approved minimum sufficient set, split for that verified hard capacity reason, or return a separate target-profile incompatibility notice.
- `Seedance 2.5，只修改现有视频8-11秒运镜，其余不变` with the source video -> compile `source + 8-11s + preserve exactly + change only + post-edit continuity`, subordinate to any stricter local-repair lock. The same request without the source video -> stop truthfully and do not claim an executable edit.
- A 2.5 complex fight or dense multi-subject interaction -> keep force direction, contact, support, occlusion, identity/voice locks, material response, and end state; simplify and bind motion/layout/clay references only when actually supplied. Split only after returned-video evidence proves execution overload and the user approves, unless a verified hard duration/reference-capacity limit already requires it.
- `苍兰诀主对标 + Seedance 2.5` -> retain project 2.39:1/24fps. Only `苍兰诀原片质感` or an equivalent explicit technical lock activates 2.40:1/25fps; model version remains orthogonal and source BT.709 is never fabricated.

### Generated-Video Repair

- Exact submitted prompt + returned video + `人物站位错、第二镜越轴、动作接不上` -> watch the complete video, cite the affected shot/timecode and visible evidence, locate the exact conflicting or missing prompt controls with a cause class and confidence, preserve successful identity/light/material controls, and return the entire six-section/nine-heading prompt with the shared global masters plus every shot's front-loaded header and performance/action/handoff rewritten in full; never return a patch, invoke an asset gate, or tell the operator to merge text.
- Exact prompt and supplied references are already clear and non-conflicting, but the returned video still drifts stochastically -> classify `MODEL_EXECUTION_VARIANCE`, make only the smallest evidence-backed normalization, and still return the entire ready-state prompt; do not fall back to a patch or add negative spam.
- A third ordinary repair changes only one performance beat -> compare the candidate with both the accepted lineage root and the immediately preceding revision; retain the exact visual baseline once, the accepted scene family, every unaffected dialogue/shot/transition/reference role/global capture-light fact/camera header, and reject the candidate if any protected item disappears.
- An ordinary unrestricted repair with actual identity, action, effect-material, motion, or audio generator references -> keep the submitted prompt and returned video diagnostic-only in the outer role table; under `参考图角色分工：`, repeat the same complete handle-bound per-reference `只锁定……，不复制……。` sentences in both the outer table and fenced first heading. The repair de-duplication pass must not shorten or delete those sentences, and the complete rewrite must not reinsert the removed operational metadata preamble.
- Exact submitted prompt + returned video + `只改镜头3，其余逐字不动，只返回改动段` -> inspect evidence only for the allowed span, preserve all frozen Unicode text internally, run the sentinel diff, and emit exactly the replacement span with no heading, wrapper fence, diagnosis, suggestion, or diff report; do not apply the full-package regression cases above.
- A preservation-first local repair whose post-splice complete fenced prompt would exceed 5000 characters -> if patch-only was explicitly requested, return only the exact replacement span; otherwise stop with `完整基线超过5000字；请授权扩大压缩范围或改为只返回替换段`. Never alter frozen text, never split it, and never deliver an over-limit complete prompt.
- Returned video + failure description but no exact submitted prompt -> describe only verified visible symptoms and request the actual prompt; do not invent a culprit clause or present a definitive full repair as if causality were known.
- Repair inspection proves that a supplied panorama cannot support the required reverse angle, interaction surface, or long-lens view -> narrow that panorama to environment/palette/material control, write the required reverse angle and interaction in direct positive text, preserve the existing actual handle, and return the complete repair without generating another image.
- The same standing, cut, action, or axis failure repeats after two retries -> stop adding prohibitions, inspect first/key/last frames and actual reference roles, then simplify causal instructions, strengthen layout/blocking text, or narrow a supplied reference role. Split only when that evidence proves execution overload and the user approves; keep any later repaired prompt's negative block at five short evidenced constraints or fewer.

### Narrative Lighting

- `暖木室内，纸窗在床榻东侧、桌面南端一盏灯笼，帝鸿与玲珑近景正反打` -> define camera and lighting requirements before direct compilation; preserve one world-space window source across reverses even though it changes screen side; keep the exact dark-side difference, practical position/relative brightness, WB, catchlight, haze, and material response continuous in one shared scene master; keep the outer summary, global masters, three shot headers, continuity lock, and QA numerically consistent, while each header's `光源` states only the active source relation/delta and each shot retains its intentionally different distance or occupancy.
- `玲珑从暗走廊走到桌边灯笼旁再停下` -> state start mark, path, threshold trigger, end light zone, and gradual visible transition while the sources and background remain fixed; reject a portrait light that follows her unchanged.
- `雪地太阳从人物后方，另加正面硬太阳和双侧轮廓` -> reject the second sun, double shadows, and unmotivated rims; use controlled passive return/negative fill to keep eyes readable.
- `纸窗柔光使用 book light` -> specify original source to bounce to separated, evenly filled final diffusion; state apparent size, output/spill consequence, visible nose-shadow edge, and relevant subtraction rather than using the equipment label alone.
- `场景图主光自右侧，但用户剧情/文字分镜明确要求左侧纸窗影与新灯笼状态` -> limit the supplied scene image to architecture/material controls, state that it does not control light direction or practical state, and directly compile the user-required left-window shadow and lantern state in text; do not generate a replacement scene image.
- Any scene with declared shots and visible eyes -> its shared light master states story function, motivated source, apparent size, world-space placement, camera-to-key relation, quality/coverage/falloff, exact dark-side difference, relevant spill boundaries, separator/practical position and relative brightness, exposure hierarchy, material response, and one stable source-motivated catchlight. Each shot header's `光源` says `本场景母版，无变化` when unchanged or only its necessary real delta when changed; if eyes are not readable, do not invent a catchlight.

### Direct Compilation And Delivery

- Sole palace panorama + identity image + exact 100mm close-up requiring table contact, recognizable columns, lamps, and previous-shot continuity -> compile immediately. Bind the identity image only to identity/outfit, bind the panorama only to architecture/palette/material/light facts it visibly supports, and use positive text for the 100mm camera position, table contact, landmark relation, and continuity. Do not block, widen the shot, or generate a replacement image.
- Supplied 玲珑/崔珏 character-design images + a palace scene-setting image + an 85mm reverse-angle written storyboard -> compile directly from those inputs. Preserve both identities, limit the scene image to truthful environment controls, and state the reverse angle, empty/occupied areas, body positions, face lighting, and interaction in text; do not enter a scene-supplement or automatic-image branch.
- A request containing only character setting + scene setting + plot, with no uploaded image -> output a complete text-generation package with `参考图角色分工：无上传素材；使用文本锁定。`; do not demand or fabricate a reference.
- A request containing character images + scene image + written four-shot storyboard -> output one reference-generation package and one fenced block containing all four shots. Never run compatibility statuses, dependency pauses, still-prompt generation, image editing/inspection, or asset hashing.
- A supplied reference with the wrong camera side for one shot -> keep its handle but explicitly exclude camera side/viewpoint from its controlling role, then describe the required camera side and visible surfaces in text.
- A required exact user-locked face, inscription, or proprietary design without source evidence -> preserve only the user's confirmed text and avoid claiming visual verification; never invent an image or bind an absent handle.
- An explicit layout/blocking board that the user actually supplies may control footprints, scale, facing, gaze arrows, action paths, and camera zones. State that it does not control identity, costume, performance rendering, lighting, material, labels, arrows, or diagram style.
- Supplied 帝鸿/玲珑 identity reference, opening embrace-support action reference, and gold-orange flame-material reference -> declare that upload order and, in both the outer `【参考素材角色表】` and fenced first heading, write the exact label `参考图角色分工：` followed by these three complete lines: `人物身份图（@图片 1）只锁定帝鸿和玲珑的脸型、发型、服装、气质，不复制棚拍背景和光感。`, `动作参考图（@图片 2）只锁定开场两人的抱扶动作、身体关系、低机位构图和洞窟正拍背景关系，不复制图中人物身份细节。`, and `火焰材质参考图（@图片 3）只锁定金橙火焰材质、透明能量丝、粒子密度、流动层次和热浪感，不复制构图。`. Do not replace them with `identity/outfit`, `motion/camera`, `style/material`, a table fragment, an English handle, or a shorter summary.
- The same three-image case explicitly targeting Seedance 2.5 -> preserve those same three handle-bound full lines in both locations; the fenced copy begins directly with `参考图角色分工：`, with no target-model metadata before it. Do not compile the first field as `@Image1只锁人物身份`, `@图片 1只锁人物身份`, `@图片 2只锁动作`, or another shortened capacity summary.
- One image + one motion video + one audio reference -> both locations contain three complete lines using `@图片 1`, `@视频 1`, and `@音频 1`; the video/audio lines include material time spans when required, and the fixed legacy heading remains `参考图角色分工：` without causing either non-image asset to disappear from lint.
- Draft generator ranges `1.0s`, `1.5s`, `1-2.5s`, `1~3s`, `1～3s`, `1—3s`, `1–3s`, `1至3秒`, and `0:03-0:06` -> reject or normalize the editable generator wording before delivery; the final copy uses only integer `N-Ns` labels.
- Generator ranges `1-3s` and `4-5s` -> pass unchanged when they match the intended whole-second beat allocation and resolved duration. A prompt that needs only `开场→接触后→镜末` causal order also passes without invented time labels.
- Returned-video diagnosis at `00:03.417` -> keep that exact inspected evidence in `【镜头设计摘要】`; never copy it into the fenced prompt. If the retry needs a timed generator beat, express that editable beat with an integer `N-Ns` label.
- Preservation-first baseline whose frozen region contains an old decimal or noncanonical range -> keep the frozen characters verbatim and validate/normalize only the authorized replacement span.
- A time-range checker must not flag `2.39:1`, model version `2.5`, or frame-accurate evidence outside the fenced copy. `T2.8`, `24fps`, and `25fps` are legitimate only outside the fence; inside it they are capture-metadata violations.
- Any generation-ready prompt -> use exactly `镜头与素材状态 → 参考素材角色表 → 镜头设计摘要 → 完整可复制提示词 → 生成前质检 → 下一轮只建议调`; retain all nine ordered inner headings with truthful `无/不适用` values where necessary, keep `负面限制` inside the same fenced block, and reject extra Jimeng workbench outer sections.
- Exact fenced counts of 3501, 4999, and 5000 characters -> all remain valid single-block prompts. The retired lower ceiling must not trigger compression, splitting, or a blocker.
- An initial complete draft above 5000 characters -> compress duplicated atmosphere, repeated global masters, workflow/meta prose, inactive physics/audio/negative channels, and verbose continuity wording in place; recount until the same invocation is at or below 5000. Preserve all nine headings, complete reference-role sentences, dialogue, performance, causal actions, positions, four-shot order, scene transitions, end-to-next-opening handoffs, continuity, one shared capture/light baseline, and every camera-header field.
- Four declared shots whose headers and performance lines make the prompt longer than an earlier project ceiling but no longer than 5000 -> retain exactly one package, one fenced block, four shot sections, four complete headers, and four complete performance/action/handoff lines. Shot count and character count never authorize splitting.
- A three-shot Seedance 2.0 prompt whose per-shot `机位/运动` fields specify three different moving-camera policies -> reject `MultiMoveOverload20`; resolve one dominant support/movement policy in the global master, permit at most one motivated moving-shot delta, and keep the other shots locked or connected through motivated cuts.
- A four-shot prompt writes camera body, fps, shutter, ISO, WB, or T-stop into the fence at all, or repeats the visible color/grain and the expanded same-scene light paragraph inside every shot -> reject it, move capture values backstage and shared visible values to the two global masters once.
- A prompt has complete global camera/light masters but no canonical header for one or more declared shots -> reject it; every shot still needs its shot label plus all five ordered shot-defining fields.
- A prompt has the shot label and five fields but changes their order, combines them across shots, writes the performance/action first, or appends an old full camera tail after performance -> reject and restore the canonical two-line shot structure.
- Split only when the selected model's verified duration limit is exceeded, its verified reference capacity is exceeded and cannot be resolved with a user-approved minimum set, the user explicitly requests separate generations, or returned-video evidence demonstrates execution overload and the user approves. Every authorized split receives a concrete handoff; no split is caused by text length or a four-shot count.
- Any prompt produced after episode retrieval -> keep episode numbers/titles, scene IDs/numbers, paths, PDF pages, line ranges, hashes, anchors, ranks, confidence, match labels, search notes, and citations out of `【完整可复制提示词】`; pass only visible current-scene acting controls.
- A reference-generation prompt -> write `直接编译（按已提供素材参考生成）` in `镜头与素材状态`; a text-only prompt -> write `直接编译（文本生成）`. Neither status claims asset inspection.
- Generic reusable 风起 style block with no reference-bound shot -> use Quick Output immediately; treat it as standalone and never prepend it verbatim to a 瑞宝PRO prompt.

## 4. Acceptance Rule

Accept the revision only when:

- every expected source hash matches or is the explicit `PENDING_HASH` placeholder for a file intentionally changed in the current maintenance turn; no release is final until all placeholders are replaced by computed SHA-256 values;
- all seventeen maintained reference hashes resolve after the final hash pass, including the always-on Eastern-fantasy television visual master, the `苍兰诀` craft reference, the two xianxia craft modules, the project craft defaults, and the Seedance 2.x CineVisible compiler; the active-effect, explicit-no-effect, 2.0, and 2.5 cases pass without changing project canon, the direct-compilation route, the sole global-master/per-shot-header camera schema, the full handle-bound per-reference role prose, or the 瑞宝PRO outer package;
- the official skill validator passes;
- all links and UI metadata resolve;
- both the character-analysis fast path and direct prompt-compilation path behave correctly, while analysis-only requests remain non-mutating and no prompt-generation route calls an automatic image workflow;
- every generation-ready response uses the 瑞宝PRO outer package while preserving all legacy prompt content inside it;
- every reference-bound response repeats the same complete per-reference `参考图角色分工：` prose in the outer role table and the self-contained fenced copy, using the correct `@图片 N / @视频 N / @音频 N` upload-order handle for those three modalities or the exact active-product handle for a 2.5-only special modality, plus one unabridged `只锁定……，不复制……。` sentence per actual supplied asset, never sacrificing it to the 5000-character in-place compression pass;
- both Seedance 2.0 and 2.5 compiler paths preserve that complete handle-bound per-reference prose; model-profile routing, time scoping, and higher reference capacity may extend a sentence but never replace it with English placeholders, role codes, or short tags;
- target-profile regression passes for explicit 2.0, explicit 2.5, dual-version, bare short SD2, otherwise-unspecified 16-30-second work, over-capacity split/stop, 2.5 extension/edit, and complex-interaction risk; every ready-state package resolves one version only, respects the verified duration/reference budget, keeps source-match values orthogonal to model version, and omits model metadata from the fenced copy;
- every target shot defines optics and narrative-lighting requirements before direct compilation, and any later change updates the same shot ID, direct text, and supplied-reference role boundaries without a gate or generated replacement asset;
- exactly one literal normative two-layer camera template exists, in `camera-parameter-presets.md`: shared capture/light masters once, then the fixed front-loaded shot header and following performance/action/handoff line. No ordinary prompt contains the retired full tail;
- every ordinary generated or fully repaired prompt satisfies `declared shot count = canonical shot-header count`; every header contains the shot label plus all five fields in the fixed order before performance, while camera body/fps/shutter/ISO/WB/color-grain, support policy, source/direction, and expanded same-scene light/material controls appear once in the global masters and are not duplicated per shot;
- every complete fenced generator block, including non-patch preservation-first repair, is measured after assembly and stays at or below 5000 characters with no overflow tolerance; an ordinary over-limit first draft is compressed in place and remeasured while all identity, complete reference roles, dialogue, performance, action, one-to-four-shot order, positions, scene transitions, handoffs, continuity, close-detail, shared global capture/light facts, and camera-header controls remain present; character count never causes splitting, while an over-limit frozen local-repair baseline stops for expanded compression-scope authorization or explicitly requested patch-only output;
- every explicit time range in complete generator text or a newly authored replacement span uses integer `N-Ns` syntax and passes duration/order/continuity checks; frame-accurate evidence stays outside the fence, qualitative timing is allowed when no range is needed, non-time decimal values are not misclassified, and frozen local-repair text is not normalized without scope;
- every backstage target record and operator-facing `【镜头设计摘要】` separates actual platform ratio, composition/crop target, synchronous-audio choice, and the camera-intent classification; every fenced copy begins directly with `参考图角色分工：` and contains no `生成模式与有效参考` line or equivalent `目标模型= / 平台画幅= / 构图目标= / 同步音频=` preamble; every Seedance 2.0 multi-shot invocation has one dominant support/movement policy with at most one motivated moving shot;
- every complete generator prompt and ordinary complete repair contains the exact `FQ-EFTV-01` quality baseline once, exactly one resolved compatible scene family, exactly one resolved effect-state branch, and current-scene light/material facts inside the existing global visual field; the fixed baseline is never removed for budget, repeated per shot, exposed by version code, or replaced by a source-title style shortcut, and the no-effect branch contains no effect vocabulary;
- every qualifying close-framing character shot passes the exact-clause placement rule, while prop/body-part inserts without a readable face and explicit closed/occluded-eye beats preserve their stated exception;
- `video-repair-loop.md` remains directly routed and hash-valid; every ordinary unrestricted repair, including execution-variance retries, uses timestamped evidence and exact or honestly uncertain cause attribution, keeps the returned video diagnostic-only, preserves successful layers, and produces a full prompt with at most five targeted negatives under project-local outer-package precedence. Explicit preservation-first repair instead passes exact-splice/patch-only and frozen-diff checks, while missing exact prompt/video evidence remains a truthful causal-attribution stop;
- no original V1 route, template, quick block, preflight item, character layer, microexpression/action system, visual rule, or camera invariant has become unreachable;
- no production prompt or repair route enters the removed asset-gating or automatic-remediation workflow, invokes an image generator, saves/inspects/hashes an automatic asset, emits asset-compatibility states, or pauses delivery for a missing scene image;
- every listed generator reference is an actual user-supplied input, receives a handle only through declared upload order, and appears with the same complete role/rejection sentence in both required prompt locations; a textual scene/character/plot/storyboard fact never becomes a fictitious asset handle;
- one-to-four-shot direct and repaired prompts remain one ready-state package and one fenced block; splitting occurs only for a verified hard duration limit, a verified hard reference-capacity limit, an explicit user request, or returned-video evidence of execution overload followed by user approval;
- `references/scripts/corpus-manifest.json` reports exactly four source PDFs, 281 source pages, episodes 1-24, 24 episode files, and 451 scenes; every source/episode/scene/root hash validates, and `scripts/locate_script_passage.py --validate` returns `PASS`.
- literal and paraphrased retrieval tests return the correct episode candidate, while ambiguous input remains low-confidence and source verification is always required.
